# Attendance-Management-System
This system replaces manual attendance using face recognition. Instead of calling names, the system:  Scans faces using a camera Recognizes students Marks attendance automatically . From your file:  Captures ~50 images per student Trains a model Stores attendance in CSV files. Required libraries include OpenCV, pandas, etc.
